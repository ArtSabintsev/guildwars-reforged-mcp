# Contributing

Read-only public Guild Wars Reforged (Guild Wars 1) sources only.

## Development

```bash
npm install
npm run typecheck
npm test
npm run build
```

Use conventional commits. Releases are automated from `main` — see [VERSIONING.md](./VERSIONING.md).
